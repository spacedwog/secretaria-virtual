declare module 'react-native-threads' {
    export class Thread {
        postMessage(message: string): void;
        onmessage: (message: string) => void;
        terminate(): void;
    }

    export class Worker extends Thread {
        constructor(scriptPath: string);
    }
}